# Linux-System
Here, you can find useful command to operate your linux server efficiently and effectively
cd > "change directory" is used to change the directory in command shell
mkdir > "make directory" is used to make the directory
